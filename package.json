{
  "name": "programacao-eventos-4rpmon",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check core.js && node --check firebase-config.js && node --check sw.js",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm test"
  }
}
